---
name: music-library-cleanup
description: 安全整理音乐文件夹：去重复（保留音质最好的一份）、把歌曲归到「歌手/专辑」目录、清理空文件夹和垃圾，全程只移动不误删、每一步都可回退，适合任何乱了套的本地或 NAS 音乐库
whenToUse: 用户想整理音乐、给音乐库去重、把乱七八糟的歌曲文件归好类、腾空间，或抱怨"歌找不到、重复一堆、目录乱"的时候
---

# 音乐文件夹整理指南

这份指南教你把一个「乱了套」的音乐文件夹，整理成一个干净、能找到每首歌的库。适用于本地硬盘、NAS（群晖/威联通/飞牛等）上的任何音乐目录。

## 先说最重要的事：三条底线

动手前，请把这三条刻在脑子里——它们决定了「整理」和「毁掉收藏」的区别：

1. **只移动，不删除。** 唯一会真正删文件的操作，必须你亲口确认。确认前，所有"要删的"文件都只是被挪到别的文件夹里放着，随时能搬回来。
2. **拿不准就留下。** 判断不了一首歌是不是重复、该归哪张专辑，就原样留着，标个"待确认"，绝对不要猜着删。
3. **先做计划，再动手。** 任何一次批量改名/搬家之前，先生成一份「哪个文件要变成什么样」的清单给你看，你点头了才执行。

## 整理前，你需要先准备好

- **找到你的音乐文件夹的完整路径**（比如 `/vol1/Music` 或 `D:\音乐`）。NAS 上常有"只能进到某个子目录"的情况，没关系，拿到完整路径就行。
- **确认电脑上有两个工具**：`ffprobe`（读歌曲信息的）和 `python3`。终端里敲 `ffprobe -version` 和 `python3 --version` 看有没有回显，没有就装一个（一般 `apt install ffmpeg` 或 `pip install`）。

## 整理六步（照着做即可）

> 本指南配套了 5 个现成的脚本，在 `scripts/` 文件夹里，下文每步会告诉你怎么运行。脚本都支持中文，把"音乐文件夹路径"换成你自己的即可。

### 第 1 步：先拍张"全家福"（留着后悔用）

把整理前所有文件的名字、大小、时间记下来，存成一个清单。以后任何时候都能拿它核对"有没有漏文件"。

```bash
cd 你的音乐文件夹路径
find . -type f -printf '%P\t%s\t%T@\n' > manifest.tsv
```

### 第 2 步：扫描每首歌的信息

用脚本把每首歌的「歌手、歌名、专辑、格式、码率」读出来，生成一个总表。

```bash
python3 scripts/build_index.py 你的音乐文件夹路径 ./
```

会生成 `index.jsonl`（歌曲信息总表）。

### 第 3 步：补齐缺失的信息（关键一步）

很多歌的文件里根本没写歌手/歌名，或者写的是盗版网站的广告。这一步把它们补回来、把广告清掉：

```bash
python3 scripts/enrich.py 你的音乐文件夹路径 ./
```

会生成 `index_enriched.jsonl`（补全后的总表）。

### 第 4 步：找出重复的歌，保留音质最好的

按「**歌名 + 歌手** 相同就算重复」来查。自动分成两类：
- **确定重复**：同名的几份，只留音质最好的一份，其余挪走（这一份由你最终确认后再删）。
- **可能是不同版本**（演唱会版、伴奏版、DJ 版、现场版……这些其实是不同的歌，不能删）：单独列一张清单给你人工看。

```bash
python3 scripts/dedup.py ./
```

会生成 `dup_auto_groups.tsv`（确定重复）、`dup_review.jsonl`（需要你人工判断的）。

### 第 5 步：统一文件名和目录

把文件名整理成统一的「歌手 - 歌名.格式」，把歌放进「歌手/专辑名/」目录里。这一步只做"计划"，不会真的动文件：

```bash
python3 scripts/build_rename_plan.py ./
```

会生成 `rename_plan.tsv`（"原路径 → 新路径"的计划清单），你确认无误后再执行。

### 第 6 步：清理空文件夹和垃圾

- 删掉整理后空掉的文件夹：`find . -depth -type d -empty -delete`
- 封面图、曲目说明（cue/log）等「只剩壳、歌已经没了」的文件夹，删之前也先问你。

最后再拍一张"全家福"，和第 1 步的对照，确认**文件总数一个没少**——这一步是"零丢失"的保证。

---

## 最容易踩的坑（每一条都是血泪教训，务必看）

| 场景 | 会出什么事 | 正确做法 |
|---|---|---|
| 歌名里带 `(Live)`、`演唱会`、`伴奏`、`DJ版`、`Remix`、`粤语版` 等字样 | 被当成"重复"误删 | 这些是**不同的版本**，是宝贝，绝不能当重复删，要列出来单独看 |
| 文件夹里有 `.trash`、`回收站`、`隔离区` 之类的目录 | 误以为"这里面的是该删的"或反过来"该留的最好版本在这" | **主目录才是唯一权威**；垃圾桶里再高清也是候删对象，正确做法是把它搬回主目录，而不是反过来删主目录 |
| 文件名是「歌名 - 歌手」倒着写的 | 歌手和歌名颠倒，放错目录 | 用读出来的"歌手"字段核对目录名；倒序时" - "右边才是歌手 |
| 歌名/歌手字段里是盗版论坛名、网址、收藏者名字 | 污染了归类依据 | 先识别这类"广告词"字样，识别到就改用文件名来判断 |
| 歌名后有 `.旺旺:xxx`、`(2)`、`(3)` 这种下载残留 | 同一首歌对不上、查不出重复 | 查重前先把这些"下载垃圾后缀"去掉再看 |
| 歌手名带年份前缀 `(2000)陈慧娴`、错别字、繁体简体混用 | 同一歌手对不上 | 年份前缀可以安全去掉；错别字/繁简宁可不合并（漏删比误删安全）|
| 读歌信息时字段名大小写不一致（`Artist`/`artist`/`ARTIST`）| 读不到歌手 | 一律忽略大小写去读 |
| 个别文件名特别长（几百字节的广告刷屏名）| 移动时报"文件名过长" | 跳过它、单独记下来，别硬改 |
| 让一堆 AI 助手并行处理时 | 有的卡死、有的全填"不确定" | 一次只让一个助手专注处理一个歌手，并要求它直接把结果存成文件、别啰嗦 |

---

## 配套脚本速查

脚本都在 `scripts/` 文件夹里，按下面顺序用（上一个的输出喂给下一个）：

| 脚本 | 干什么 | 运行命令 |
|---|---|---|
| `build_index.py` | 扫描所有歌的基本信息 | `python3 build_index.py 音乐库路径 输出目录` |
| `enrich.py` | 补齐/清洗歌手歌名专辑 | `python3 enrich.py 音乐库路径 输出目录` |
| `dedup.py` | 找重复、留最优音质 | `python3 dedup.py 输出目录` |
| `build_rename_plan.py` | 生成改名/归类计划（不动文件）| `python3 build_rename_plan.py 输出目录` |
| `apply_album_mapping.py` | 按"歌→专辑"清单把歌搬进目录 | `python3 apply_album_mapping.py 歌手目录路径 映射.json` |

> 脚本里遇到的几个"约定"（比如"未知专辑"目录名、回收站目录名），不同人的库可能叫法不一样，遇到对不上时按你的实际情况在脚本开头或说明处微调即可。

---

## 收工前的自检

- [ ] 整理前后文件总数一致 → 一个没丢
- [ ] 每个被删的文件，都找得到它"保留下来的更好版本"
- [ ] 拿不准的都标了"待确认"，没有擅自处理
- [ ] 每一步搬家/删除都有记录，出错能改回来

**记住：整理音乐是一场"宁可慢、不可错"的活。遇到不确定，多留一份、多问一句，永远好过手快删错一首歌。**
